version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
    restart: always
    environment:
      - DEBUG=False
      - ALLOWED_HOSTS=192.168.1.94,localhost,127.0.0.1